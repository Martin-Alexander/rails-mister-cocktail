<h1>Cocktails</h1>

<%= link_to "Add your own", new_cocktail_path, class: "btn btn-primary" %>

<% @cocktails.each do |cocktail| %>
  <div>
    <h3><%= link_to(cocktail.name, cocktail_path(cocktail)) %></h3>
  </div>
<% end %>
